name: Bygg og Deploy Rekrutteringsbistand-frontend
on:
  push:
  workflow_dispatch: # Legger til mulighet for manuell kjøring

jobs:
  build-and-deploy:
    name: Next
    uses: navikt/toi-github-actions-workflows/.github/workflows/build-and-deploy-nextjs.yaml@v9.2
    with:
      pnpm: true
      a11y: true
      storybook: true
      playwright: true
      playwright-github-report: true
      dev-vars-path: '.nais/dev-gcp.json'
      dev2-vars-path: '.nais/dev2-gcp.json'
      prod-vars-path: '.nais/prod-gcp.json'
      deploy-dev2-branch: 'storybook-ansatt'
    secrets: inherit
    permissions:
      contents: write
      id-token: write
      security-events: write
      pages: write
